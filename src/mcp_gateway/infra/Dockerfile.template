FROM python:3.12-slim
ARG TEMPLATE
ARG PORT=8000
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn httpx
COPY ${TEMPLATE} server.py
EXPOSE ${PORT}
CMD ["sh", "-c", "uvicorn server:app --host 0.0.0.0 --port ${PORT}"]
